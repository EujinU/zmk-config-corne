name: ZMK Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Для ручного запуска

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config-dir: config/  # ← ЗАМЕНИЛИ keymap-dir на config-dir
    secrets: inherit
